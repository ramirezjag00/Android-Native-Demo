// classes can be inherited and we use class for the superclass and extends for subclass
// interfaces are like classes, states all the required/enforced methods where it was implemented and we use interface for the superclass and implements for subclass
// Basically java interfaces are swift’s protocols / delegates.
// interface can only contain method headers and class constants

public interface Shape {
  public double CalculateArea();
  public double CalculatePerimeter(); 
}
